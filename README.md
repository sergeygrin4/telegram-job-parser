# Telegram Job Parser

Слушает чаты и каналы Telegram, ищет вакансии и уведомляет менеджера.

## 🚀 Установка и запуск

1. Клонируй репозиторий:
   ```bash
   git clone https://github.com/yourusername/telegram-job-parser.git
   cd telegram-job-parser
Создай виртуальное окружение:

bash
Копировать код
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
Установи зависимости:

bash
Копировать код
pip install -r requirements.txt
Создай файл .env (на основе .env.sample):

bash
Копировать код
cp .env.sample .env
Заполни нужные значения (TG_API_ID, TG_API_HASH, MANAGER_ID).

Запусти парсер:

bash
Копировать код
python telegram_parser.py
При первом запуске введи свой номер телефона и код подтверждения из Telegram.

Файл job_parser.session сохранит авторизацию — его нельзя коммитить.

yaml
Копировать код

---

## ✅ Дальше
После того как создашь `.env` и запустишь:
```bash
python telegram_parser.py
Парсер авторизуется (один раз) и начнёт слушать все чаты, где ты состоишь.
Если хочешь ограничить список — добавь ID или username в MONITORED_CHATS
